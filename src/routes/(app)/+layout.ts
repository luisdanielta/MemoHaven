import { isAuthenticated } from '$lib/context/pb.svelte';
import { redirect } from '@sveltejs/kit';
/*
export const load = async () => {
	if (!isAuthenticated()) return redirect(307, '/sign/in');
};
*/
